# Valores usados quando o Vite roda em modo production (npm run build).
# Este arquivo ENTRA no repositório por padrão (não é segredo, é configuração).
# Para desenvolvimento local use .env (gitignorado), que aponta para localhost.

VITE_API_URL=https://club12-api.onrender.com
